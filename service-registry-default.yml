server:
  port: 8761

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false  # override global
  client:
    register-with-eureka: false  # override global
    fetch-registry: false        # override global
    service-url:
      defaultZone: http://localhost:8761/eureka/
